// para rodar o servidor, executar o comando json-server --watch --host [seu IP] db.json
// para rodar o servidor c/ autenticação, executar o comando json-server-auth --watch --host [seu IP] db.json
// para rodar o servidor c/ autenticação e levando em consideração as configurações das autroizações das rotas
// é preciso adicionar o -r, executar o comando json-server-auth --watch --host [seu IP] db.json -r [nome do arquivo da configuração das rotas] routes.json
// definir o ip baseado no IP4 do ipconfig